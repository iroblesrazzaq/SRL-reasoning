[project]
name = "deep-learning-srl"
version = "0.1.0"
description = "Supervised Reinforcement Learning data processing pipeline"
requires-python = ">=3.8"
dependencies = [
    "datasets>=2.14.0",
    "tqdm>=4.65.0",
]

[tool.hatch.build.targets.wheel]
packages = []

